[project]
name = "safetensors-arch"
version = "0.3.0"
description = "Tell what a .safetensors file actually is, by its tensor table — not its filename or vendor metadata."
readme = "README.md"
authors = [{ name = "Jiahao Xu" }]
requires-python = ">=3.9"
license = { text = "MIT" }
keywords = ["safetensors", "lora", "diffusion", "sdxl", "model-inspection"]
classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: MIT License",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = []

[project.urls]
Homepage = "https://github.com/JiahaoXu10Arthur/safetensors-arch"
Issues = "https://github.com/JiahaoXu10Arthur/safetensors-arch/issues"

[project.optional-dependencies]
test = ["pytest>=7"]

[project.scripts]
safetensors-arch = "safetensors_arch.__main__:main"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
